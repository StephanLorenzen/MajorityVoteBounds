\documentclass{article}
\usepackage{pgfplotstable}
%\pgfplotsset{compat=1.16}
\usepackage{changepage}
\usepackage{booktabs}           % for table rules
%\usepackage{siunitx}			% for precision
\input{../../colors.tex}

% Setup siunitx:
%\sisetup{
%	round-mode          = places, % Rounds numbers
%	round-precision     = 3, % to 2 places
%	per-mode        = symbol,
%}

\begin{document}
\begin{table}[th!]
	\hskip-3.0cm
	\pgfplotstabletypeset[
		col sep=comma,
		string type,
		columns = {dataset, lam, tnd, mu, bern},
		columns/dataset/.style={string type, column name=Dataset, column type={l}},
		%columns/unf/.style={column name=$\hat L(\MV_{u}\mbox{,}\testset)$, column type={c}},
		columns/lam/.style={column name=$\hat  L(\MV_{\rho_{\FO}^*}\mbox{,}\testset)$, column type={c}},
		columns/tnd/.style={column name=$L(\MV_{\rho_{\TND}^*}\mbox{,}\testset)$, column type={c}},
		columns/mu/.style={column name=$L(\MV_{\rho_{\MU}^*}\mbox{,}\testset)$, column type={c}},
		columns/bern/.style={column name=$L(\MV_{\rho_{\Ben}^*}\mbox{,}\testset)$, column type={c}},
		every head row/.style={before row=\toprule,after row=\midrule},
		every last row/.style={after row=\bottomrule},
	]{optimize/bounds_optimized.csv}
\caption{Bound}
\end{table}


\end{document}
